$kind: http-request
name: Get All Students (filter by schoolId)
url: "{{baseUrl}}/students?schoolId=school-001"
method: GET
queryParams:
  schoolId: school-001
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1773227810904
