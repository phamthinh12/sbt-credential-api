$kind: http-request
name: Create Credential
url: "{{baseUrl}}/credentials"
method: POST
body:
  type: formdata
  content: []
auth:
  credentials:
    - key: token
      value: "{{token}}"
  type: bearer
order: 68
